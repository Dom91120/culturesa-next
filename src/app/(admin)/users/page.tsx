export default function UsersPage() {
  return (
    <div className="panel">
      <div className="panel-title">
        <span className="dot" />
        Comptes utilisateurs
      </div>
      <p style={{ fontSize: ".85rem", color: "var(--muted)" }}>
        Gestion des comptes — à venir (phase suivante). Tableau des utilisateurs avec recherche,
        rôles et indicateurs RGPD.
      </p>
    </div>
  );
}
