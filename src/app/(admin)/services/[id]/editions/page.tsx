export default function EditionsPage() {
  return (
    <div className="panel">
      <div className="panel-title">
        <span className="dot" />
        Éditions
      </div>
      <p style={{ fontSize: ".85rem", color: "var(--muted)" }}>
        Liste imprimable des réservations (export CSV / PDF) — à venir.
      </p>
    </div>
  );
}
