export default function MessageriePage() {
  return (
    <div className="panel">
      <div className="panel-title">
        <span className="dot" />
        Messagerie
      </div>
      <p style={{ fontSize: ".85rem", color: "var(--muted)" }}>
        Configuration des e-mails (SMTP, modèles de messages) — à venir.
      </p>
    </div>
  );
}
